# Neon Postgres — used on Vercel (connection pooling via pgBouncer)
DATABASE_URL="postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/travelr?sslmode=require&pgbouncer=true&connect_timeout=15"

# Neon direct connection — used for migrations (bypasses pgBouncer)
DIRECT_URL="postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/travelr?sslmode=require"

# Claude AI (get yours at console.anthropic.com)
ANTHROPIC_API_KEY="sk-ant-..."

NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
